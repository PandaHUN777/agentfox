.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.db
*.db-wal
*.db-shm
var/
node_modules/
# `uv build` writes here. The release workflow builds on a runner, but a local
# `uv build` should not leave artefacts staged for commit.
/dist/
dashboard/.next/
dashboard/.npm-cache/
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example
# Written by `agentfox init` when run from the repo root without an explicit
# --path — a local dev convenience file, not a repo artifact.
/nometria.toml
