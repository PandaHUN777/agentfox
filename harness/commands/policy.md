---
description: Draft, validate, lint and simulate a policy change — promotion only on explicit approval
argument-hint: "<what the policy should do>"
---
Load the `author-policy` skill and follow it for this goal: $ARGUMENTS

Stop after simulation and present the diff. Do not run `policy enforce` unless the user
explicitly says to in reply.
