---
description: Safe offline tour of Nometria in a throwaway database
---
Load the `tour-product` skill from this plugin and follow it. Use a scratch database. Keep
`--skip-sessions` on quickscan and never pass `--submit`.
