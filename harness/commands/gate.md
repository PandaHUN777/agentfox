---
description: Set up an eval regression gate and a governance CI workflow
argument-hint: "[suite]"
---
Load the `eval-gate` skill and follow it. Suite: `$ARGUMENTS` (if empty, list what exists
and ask which one, or use `support-quality` in a scratch DB to demonstrate).
