---
description: Triage open findings into a grouped, prioritised action list
argument-hint: "[critical|high|medium|low]"
---
Load the `triage-findings` skill and follow it. If `$ARGUMENTS` names a severity, filter to
it. Propose status changes; apply them only after the user agrees.
